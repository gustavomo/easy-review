---
phase: 02-ai-review-generation
plan: 09
type: execute
wave: 7
depends_on: [02-08]
files_modified: []
autonomous: false
requirements: [REV-01, REV-02, REV-03, REV-04, REV-05, VIEW-01, VIEW-02, VIEW-03, PROJ-01, PROJ-02, PROJ-03]

must_haves:
  truths:
    - "User can right-click a PR and select Generate Review to start generation"
    - "Webview panel opens in ViewColumn.Two and shows streaming output in real time"
    - "Completed review displays 6 collapsible sections"
    - "Findings section shows severity-colored cards"
    - "History dropdown lists all reviews for the PR"
    - "Project analysis runs and completes with a summary notification"
    - "All unit tests green"
  artifacts:
    - path: "src/easy-review/panel/ReviewPanel.ts"
      provides: "Full singleton review panel in working extension"
      contains: "ReviewPanel"
    - path: "src/webview/ReviewDocument.tsx"
      provides: "6-section display in Extension Development Host"
      contains: "ReviewDocument"
  key_links: []
---

<objective>
Human verification in the VS Code Extension Development Host. Confirms the full Phase 2 review generation pipeline works end-to-end: trigger, streaming, complete display, history, and project analysis.

Purpose: Phase 2 unit tests cannot cover VS Code webview context. This checkpoint validates the real user experience.
Output: Verified Phase 2 functionality or a list of issues to feed back into gap closure.
</objective>

<execution_context>
@/Users/gustavo.moreno/Documents/personal info/easy-review/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gustavo.moreno/Documents/personal info/easy-review/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-ai-review-generation/02-CONTEXT.md
@.planning/phases/02-ai-review-generation/02-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite and build before human verification</name>
  <files></files>
  <read_first>
    - .planning/phases/02-ai-review-generation/02-VALIDATION.md (per-task verification map and sampling rate)
  </read_first>
  <action>
Run the full test suite and both build targets. All must pass before proceeding to human verification.

```bash
npm run test:unit
npm run build:extension
npm run build:webview
```

Fix any failures before proceeding. The human verification step should only happen against a clean build.
  </action>
  <verify>
    <automated>cd "/Users/gustavo.moreno/Documents/personal info/easy-review" && npm run test:unit && npm run build:extension && npm run build:webview && echo "ALL BUILDS PASS"</automated>
  </verify>
  <done>npm run test:unit exits 0, npm run build:extension exits 0, npm run build:webview exits 0.</done>
  <acceptance_criteria>
    - `npm run test:unit` exits 0 with no failing tests
    - `npm run build:extension` exits 0 with no TypeScript errors
    - `npm run build:webview` exits 0 with no Vite build errors
    - No `it.todo` stubs remain in any test file (grep "it.todo" src/test/unit/*.test.ts returns no matches across the 5 Phase 2 test files)
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Full Phase 2 AI review generation pipeline:
    - easyReview.generateReview command wired to PR tree context menu
    - ReviewPanel singleton in ViewColumn.Two with 4-state machine
    - Streaming Claude/Codex CLI output batched to webview every 200ms
    - Completed review displayed as 6 collapsible sections with severity-colored findings
    - Reviews persisted to SQLite with history dropdown
    - Project analysis command collecting workspace files + git log
    - PR history analysis fetching last 100 PRs via GitHub API
  </what-built>
  <how-to-verify>
    **Setup:** Open the easy-review workspace. Press F5 (Start Debugging) to launch the Extension Development Host.

    **Test 1 — Review generation (REV-01, REV-03, VIEW-01):**
    1. In the Extension Development Host, open the Easy Review sidebar panel
    2. Add a PR if none are listed (use "Easy Review: Add PR by URL")
    3. Right-click a PR in the list — verify "Generate Review" appears in the context menu
    4. Click "Generate Review"
    5. Verify the Easy Review webview panel opens in ViewColumn.Two
    6. Verify streaming text appears in the panel in real time (not blank)
    7. Verify the elapsed timer shows in the header ("Generating... Ns")
    8. Verify the Cancel Generation button appears during generation

    **Test 2 — Completed review display (REV-02, VIEW-01, VIEW-02):**
    9. Wait for generation to complete
    10. Verify the completed review shows 6 collapsible sections with headings: Executive Summary, Categorized Changes, Key Code Changes, Findings, Impact Analysis, Mermaid Diagram
    11. Click any section title — verify it collapses/expands with chevron icon change
    12. Verify the Findings section shows severity-colored cards (critical=red, warning=yellow, suggestion=blue)
    13. Verify the Mermaid section shows a code block with the deferral note below it
    14. Verify the model badge in the header shows "claude" or "codex"

    **Test 3 — Review history (REV-04, REV-05, VIEW-03):**
    15. Right-click the same PR again — verify the re-generate confirmation dialog appears
    16. Click "Generate New Review" to proceed
    17. After completion, verify the history dropdown in the header shows two entries ("Review 1 (date)", "Review 2 (date)")
    18. Select "Review 1" from the dropdown — verify the earlier review loads

    **Test 4 — Cancel generation (D-04):**
    19. Right-click a PR and select Generate Review
    20. During streaming, click "Cancel Generation"
    21. Verify the webview returns to Idle state ("No review generated yet")
    22. Verify no partial review was persisted

    **Test 5 — Project analysis (PROJ-01, PROJ-02, PROJ-03):**
    23. Open the Command Palette (Cmd+Shift+P)
    24. Run "Easy Review: Analyze Project"
    25. Verify the progress notification appears in the notification area
    26. Verify the completion notification shows: "Project analysis complete. Collected: README.md, N source files, M recent commits."
    27. Generate a review for any PR — verify the project context is included in the prompt (check Output Channel for the prompt content, or verify the review quality improved)

    **Test 6 — Error handling:**
    28. Temporarily set easyReview.claudePath to an invalid path in settings
    29. Try to generate a review — verify the error state appears in the webview with "Review generation failed" heading and "Retry Review" button
    30. Restore the correct claudePath setting
    31. Click "Retry Review" — verify generation restarts successfully
  </how-to-verify>
  <resume-signal>
    Type "approved" if all 6 tests pass.
    Type "issues: [list]" if any tests fail — include the test number and observed behavior.
    Issues will be triaged into gap closure plans.
  </resume-signal>
</task>

</tasks>

<verification>
Phase 2 is complete when all 6 human verification tests pass and the automated test suite is green.
</verification>

<success_criteria>
All Phase 2 requirements verified:
- REV-01: Generate Review right-click command works
- REV-02: 6-section structured output displayed
- REV-03: Real-time streaming visible during generation
- REV-04: Reviews persisted to SQLite
- REV-05: Re-generation appends new review; history preserved
- VIEW-01: Dedicated webview panel with full 6-section format
- VIEW-02: Findings displayed by severity with correct colors
- VIEW-03: History dropdown shows all review versions
- PROJ-01: Project analysis collects workspace files
- PROJ-02: PR history analysis fetches last 100 PRs
- PROJ-03: Project analysis prepended to review prompt when available
</success_criteria>

<output>
After completion, create `.planning/phases/02-ai-review-generation/02-09-SUMMARY.md`
</output>
