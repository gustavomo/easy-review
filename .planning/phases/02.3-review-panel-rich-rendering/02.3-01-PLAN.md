---
phase: 02.3-review-panel-rich-rendering
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - vitest.config.ts
  - src/webview/parseCategorizedChanges.ts
  - src/webview/parseCategorizedChanges.test.ts
  - src/webview/parseImpactAnalysis.ts
  - src/webview/parseImpactAnalysis.test.ts
autonomous: true
requirements:
  - POL-01
  - D-10
  - D-13
  - D-14
  - D-17
  - D-18

must_haves:
  truths:
    - "parseCategorizedChanges returns Category[] from markdown with ### headings and bullet items"
    - "parseCategorizedChanges returns null when content has no ### headings (triggers fallback)"
    - "parseImpactAnalysis returns Dimension[] from markdown with ### headings, High/Medium/Low keywords"
    - "parseImpactAnalysis sets hasHighBreaking=true when a Breaking Changes dimension has High impact"
    - "parseImpactAnalysis returns null when content has no ### headings (triggers fallback)"
    - "npm run test:unit exits 0 with new webview test files included"
  artifacts:
    - path: "src/webview/parseCategorizedChanges.ts"
      provides: "Pure parsing function for CategorizedChangesSection"
      exports: ["parseCategorizedChanges", "Category"]
    - path: "src/webview/parseCategorizedChanges.test.ts"
      provides: "Unit tests for parseCategorizedChanges"
    - path: "src/webview/parseImpactAnalysis.ts"
      provides: "Pure parsing function for ImpactAnalysisSection"
      exports: ["parseImpactAnalysis", "Dimension"]
    - path: "src/webview/parseImpactAnalysis.test.ts"
      provides: "Unit tests for parseImpactAnalysis"
    - path: "vitest.config.ts"
      provides: "Updated include array to cover src/webview/**/*.test.ts"
      contains: "src/webview/**/*.test.ts"
  key_links:
    - from: "vitest.config.ts"
      to: "src/webview/**/*.test.ts"
      via: "include array"
      pattern: "src/webview/\\*\\*\\.test\\.ts"
    - from: "src/webview/CategorizedChangesSection.tsx"
      to: "src/webview/parseCategorizedChanges.ts"
      via: "import"
      pattern: "import.*parseCategorizedChanges"
    - from: "src/webview/ImpactAnalysisSection.tsx"
      to: "src/webview/parseImpactAnalysis.ts"
      via: "import"
      pattern: "import.*parseImpactAnalysis"
---

<objective>
Wave 0 gate: create the pure parsing functions and their unit tests before any React components exist.

Purpose: CategorizedChangesSection and ImpactAnalysisSection (Plan 04) import these functions to extract structured data from markdown content. Extracting parsing logic into framework-free functions makes them unit-testable under Vitest's node environment, where React components cannot run.

Output: two parsing modules + two test files + vitest config extended to include src/webview/**/*.test.ts
</objective>

<execution_context>
@/Users/gustavo.moreno/Documents/personal info/easy-review/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gustavo.moreno/Documents/personal info/easy-review/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02.3-review-panel-rich-rendering/02.3-CONTEXT.md
@.planning/phases/02.3-review-panel-rich-rendering/02.3-RESEARCH.md
@.planning/phases/02.3-review-panel-rich-rendering/02.3-UI-SPEC.md

<interfaces>
<!-- Types the executor produces — consumed by Plan 04 -->

parseCategorizedChanges.ts must export:
```typescript
export interface Category {
  name: string;
  items: string[];
}

/**
 * Parse markdown content with ### heading + bullet structure into Category[].
 * Returns null when no ### headings are found (caller falls back to marked()).
 */
export function parseCategorizedChanges(content: string): Category[] | null
```

parseImpactAnalysis.ts must export:
```typescript
export type ImpactLevel = 'high' | 'medium' | 'low' | null;

export interface Dimension {
  name: string;
  impact: ImpactLevel;
  items: string[];
}

export interface ParsedImpactAnalysis {
  dimensions: Dimension[];
  hasHighBreaking: boolean;
}

/**
 * Parse markdown content with ### heading + bullet structure into ParsedImpactAnalysis.
 * Returns null when no ### headings are found (caller falls back to marked()).
 */
export function parseImpactAnalysis(content: string): ParsedImpactAnalysis | null
```

Existing vitest.config.ts include array (must extend, not replace):
```typescript
include: ['src/test/unit/**/*.test.ts', 'src/easy-review/**/*.test.ts'],
```
After change:
```typescript
include: [
  'src/test/unit/**/*.test.ts',
  'src/easy-review/**/*.test.ts',
  'src/webview/**/*.test.ts',
],
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: parseCategorizedChanges — pure function + unit tests</name>
  <files>src/webview/parseCategorizedChanges.ts, src/webview/parseCategorizedChanges.test.ts, vitest.config.ts</files>
  <read_first>
    - vitest.config.ts — see current include array before modifying
    - src/webview/FindingCard.tsx — reference badge/severity pattern conventions
    - .planning/phases/02.3-review-panel-rich-rendering/02.3-RESEARCH.md — Pattern 3 (category parsing strategy)
    - .planning/phases/02.3-review-panel-rich-rendering/02.3-CONTEXT.md — D-11, D-12, D-13
  </read_first>
  <behavior>
    - Test 1 (parse success): content with `### Feature\n- item a\n- item b\n### Bug Fix\n- item c` → returns `[{name:'Feature', items:['item a','item b']}, {name:'Bug Fix', items:['item c']}]`
    - Test 2 (fallback trigger): content with no `###` headings (plain bullet list markdown) → returns `null`
    - Test 3 (empty content): empty string `''` → returns `null`
    - Test 4 (mixed content with leading text): content starting with prose then `### Refactor\n- item` → captures categories only, not prose
    - Test 5 (bullet variants): `*` bullets as well as `-` bullets both parsed into items array
  </behavior>
  <action>
Step 1 — Extend vitest.config.ts include array.

Read vitest.config.ts. The current include array is:
```typescript
include: ['src/test/unit/**/*.test.ts', 'src/easy-review/**/*.test.ts'],
```
Change to:
```typescript
include: [
  'src/test/unit/**/*.test.ts',
  'src/easy-review/**/*.test.ts',
  'src/webview/**/*.test.ts',
],
```
All other config stays unchanged.

Step 2 — Create src/webview/parseCategorizedChanges.test.ts FIRST (RED phase).

Write tests using the behavior block above. Import from `./parseCategorizedChanges`. Run `npm run test:unit` — tests MUST fail because the source file does not exist yet.

Step 3 — Create src/webview/parseCategorizedChanges.ts (GREEN phase).

Export the Category interface and parseCategorizedChanges function:

```typescript
export interface Category {
  name: string;
  items: string[];
}

export function parseCategorizedChanges(content: string): Category[] | null {
  const lines = content.split('\n');
  const categories: Category[] = [];
  let current: Category | null = null;

  for (const line of lines) {
    const heading = line.match(/^###\s+(.+)/);
    if (heading) {
      if (current) categories.push(current);
      current = { name: heading[1].trim(), items: [] };
      continue;
    }
    if (current) {
      // Accept - and * bullet variants
      const bullet = line.match(/^\s*[-*]\s+(.+)/);
      if (bullet) {
        current.items.push(bullet[1].trim());
      }
    }
  }
  if (current) categories.push(current);

  return categories.length > 0 ? categories : null;
}
```

Run `npm run test:unit` — tests MUST pass (GREEN).
  </action>
  <verify>
    <automated>cd "/Users/gustavo.moreno/Documents/personal info/easy-review" && npm run test:unit -- --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - vitest.config.ts include array contains the string `src/webview/**/*.test.ts`
    - src/webview/parseCategorizedChanges.ts exports `Category` interface and `parseCategorizedChanges` function
    - `grep -n "export function parseCategorizedChanges" src/webview/parseCategorizedChanges.ts` returns a match
    - `grep -n "export interface Category" src/webview/parseCategorizedChanges.ts` returns a match
    - src/webview/parseCategorizedChanges.test.ts exists with at least 5 test cases
    - `npm run test:unit` exits 0 (all tests pass, including new ones)
  </acceptance_criteria>
  <done>parseCategorizedChanges.ts exports Category interface and parseCategorizedChanges function; all 5 test cases pass; vitest config extended to cover src/webview/**/*.test.ts</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: parseImpactAnalysis — pure function + unit tests</name>
  <files>src/webview/parseImpactAnalysis.ts, src/webview/parseImpactAnalysis.test.ts</files>
  <read_first>
    - src/webview/parseCategorizedChanges.ts — follow same parsing pattern
    - .planning/phases/02.3-review-panel-rich-rendering/02.3-RESEARCH.md — Pattern 4 (impact parsing, hasHighBreaking)
    - .planning/phases/02.3-review-panel-rich-rendering/02.3-CONTEXT.md — D-14, D-15, D-16, D-17, D-18
    - .planning/phases/02.3-review-panel-rich-rendering/02.3-UI-SPEC.md — ImpactAnalysisSection spec
  </read_first>
  <behavior>
    - Test 1 (parse success): content with `### Performance\nHigh impact\n- item` → returns `{dimensions:[{name:'Performance', impact:'high', items:['item']}], hasHighBreaking:false}`
    - Test 2 (hasHighBreaking true): content with `### Breaking Changes\nHigh impact\n- removed API` → returns `{dimensions:[...], hasHighBreaking:true}`
    - Test 3 (hasHighBreaking false for low): content with `### Breaking Changes\nLow impact\n- minor` → `hasHighBreaking:false`
    - Test 4 (medium detection): content with `### Security\nMedium impact\n- item` → `impact:'medium'`
    - Test 5 (no headings fallback): plain markdown without `###` headings → returns `null`
    - Test 6 (case insensitive impact): `HIGH`, `High`, `high` all resolve to `'high'`
  </behavior>
  <action>
Step 1 — Create src/webview/parseImpactAnalysis.test.ts FIRST (RED phase).

Write tests using the behavior block above. Import from `./parseImpactAnalysis`. Run `npm run test:unit` — tests MUST fail.

Step 2 — Create src/webview/parseImpactAnalysis.ts (GREEN phase).

```typescript
export type ImpactLevel = 'high' | 'medium' | 'low' | null;

export interface Dimension {
  name: string;
  impact: ImpactLevel;
  items: string[];
}

export interface ParsedImpactAnalysis {
  dimensions: Dimension[];
  hasHighBreaking: boolean;
}

function detectImpact(lines: string[]): ImpactLevel {
  const text = lines.join(' ').toLowerCase();
  if (text.includes('high')) return 'high';
  if (text.includes('medium')) return 'medium';
  if (text.includes('low')) return 'low';
  return null;
}

export function parseImpactAnalysis(content: string): ParsedImpactAnalysis | null {
  const lines = content.split('\n');
  const dimensions: Dimension[] = [];
  let current: { name: string; bodyLines: string[] } | null = null;

  const flush = () => {
    if (!current) return;
    const items: string[] = [];
    for (const l of current.bodyLines) {
      const bullet = l.match(/^\s*[-*]\s+(.+)/);
      if (bullet) items.push(bullet[1].trim());
    }
    dimensions.push({
      name: current.name,
      impact: detectImpact(current.bodyLines),
      items,
    });
    current = null;
  };

  for (const line of lines) {
    const heading = line.match(/^###\s+(.+)/);
    if (heading) {
      flush();
      current = { name: heading[1].trim(), bodyLines: [] };
      continue;
    }
    if (current) {
      current.bodyLines.push(line);
    }
  }
  flush();

  if (dimensions.length === 0) return null;

  const hasHighBreaking = dimensions.some(
    d => d.name.toLowerCase().includes('breaking') && d.impact === 'high',
  );

  return { dimensions, hasHighBreaking };
}
```

Run `npm run test:unit` — all tests MUST pass.
  </action>
  <verify>
    <automated>cd "/Users/gustavo.moreno/Documents/personal info/easy-review" && npm run test:unit -- --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - src/webview/parseImpactAnalysis.ts exports `ImpactLevel`, `Dimension`, `ParsedImpactAnalysis`, and `parseImpactAnalysis`
    - `grep -n "export function parseImpactAnalysis" src/webview/parseImpactAnalysis.ts` returns a match
    - `grep -n "hasHighBreaking" src/webview/parseImpactAnalysis.ts` returns a match
    - src/webview/parseImpactAnalysis.test.ts exists with at least 6 test cases
    - `npm run test:unit` exits 0 (all new tests pass)
  </acceptance_criteria>
  <done>parseImpactAnalysis.ts exports all types and function; hasHighBreaking detection works; all 6 test cases pass; npm run test:unit exits 0</done>
</task>

</tasks>

<verification>
Run `npm run test:unit -- --reporter=verbose` — output must show new webview test files discovered and all tests passing. No pre-existing test failures should be introduced.

Check vitest.config.ts: `grep "src/webview" vitest.config.ts` returns a match.
</verification>

<success_criteria>
- vitest.config.ts include array contains `src/webview/**/*.test.ts`
- parseCategorizedChanges.ts and parseImpactAnalysis.ts exist with exported types and functions
- Both test files exist with passing unit tests
- npm run test:unit exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/02.3-review-panel-rich-rendering/02.3-01-SUMMARY.md`
</output>
