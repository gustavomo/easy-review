# Phase 02.3: Review Panel Rich Rendering - Context

**Gathered:** 2026-04-04
**Status:** Ready for planning

<domain>
## Phase Boundary

Upgrade the review webview panel to render code blocks with syntax highlighting and
Mermaid source as real rendered SVG diagrams. Everything else (layout, sections,
findings, streaming) is out of scope — this phase only affects how content inside
sections is displayed.

</domain>

<decisions>
## Implementation Decisions

### Syntax highlighting library
- **D-01:** Use **highlight.js** — integrate via marked's `highlight` callback option.
- **D-02:** Apply to ALL markdown sections globally (Executive Summary, Architecture,
  Findings body, every section). One global callback on `marked` config — no per-section
  logic.
- **D-03:** Auto-detect language when fenced code block has no language tag.
- **D-04:** Style with highlight.js CSS vars mapped to VS Code theme colors (not hardcoded
  colors). Use `--vscode-editor-foreground`, `--vscode-textCodeBlock-background` etc.
  Avoid importing a hljs theme CSS directly — it would override VS Code theming.

### Mermaid rendering
- **D-05:** Full SVG rendering via **mermaid.js** bundled into the webview.
- **D-06:** The dedicated Mermaid section (`isMermaidSection` in ReviewDocument.tsx)
  renders as a live SVG diagram. The current "coming in a future version" note is removed.
- **D-07:** Mermaid needs `unsafe-eval` in the CSP `script-src` directive (it uses `eval`
  internally). Update `getWebviewHtml()` in ReviewPanel.ts to add `'unsafe-eval'` to
  `script-src` for the mermaid use case.
- **D-08:** Mermaid theme: use `'dark'` or `'neutral'` — detect from VS Code's active
  color theme if possible (check `document.body.className` for `vscode-dark` / `vscode-light`),
  otherwise default to `'dark'`.
- **D-09:** Render errors (invalid diagram syntax) show a styled error message in place of
  the diagram — do not crash the whole section.

### Claude's Discretion
- Exact highlight.js CSS overrides for token colors
- Whether to lazy-load mermaid (import on first render) or eager-load
- Exact error UI for mermaid render failures

</decisions>

<specifics>
## Specific Ideas

- User said "visualize as a code block" — implies proper syntax coloring, not just
  monospace background (which already exists in the CSS).
- User said "visualize the mermaid diagram" — confirmed full SVG render, not a copy-button
  workaround.

</specifics>

<canonical_refs>
## Canonical References

No external specs — requirements are fully captured in decisions above.

### Existing implementation files to read before planning
- `src/webview/ReviewDocument.tsx` — Current rendering logic; has `isMermaidSection` branch
  and `marked()` call for all other sections. This is the primary file to modify.
- `src/webview/webview.css` — Existing `.easy-review-md pre code` styles; any hljs overrides
  must not conflict with these.
- `src/easy-review/panel/ReviewPanel.ts` — `getWebviewHtml()` method; CSP string must be
  updated to add `'unsafe-eval'` for mermaid.
- `src/webview/index.tsx` — Webview entry point; check if mermaid init belongs here.
- `vite.config.ts` (webview build) — Bundle size will increase ~2MB for mermaid; verify
  no chunk size warnings block the build.

### Requirement references
- `POL-01` in `.planning/REQUIREMENTS.md` — "Mermaid diagrams in reviews are rendered
  visually in the webview" — this phase delivers POL-01.

</canonical_refs>

<code_context>
## Existing Code Insights

### Reusable Assets
- `marked` (already installed) — used in ReviewDocument.tsx for all section markdown.
  highlight.js integrates via `marked.setOptions({ highlight: ... })` or
  `marked.use({ hooks: { ... } })` depending on marked version.
- `.easy-review-md` CSS class — applied to all markdown divs; any hljs token colors
  scoped inside this class avoid polluting other webview content.

### Established Patterns
- VS Code CSS vars (`--vscode-*`) used throughout for theming — hljs token colors must
  follow this pattern, not import static CSS.
- `dangerouslySetInnerHTML={{ __html: marked(content) }}` — current render path; will
  need to become async if marked highlight callback is async (hljs is sync, so it's fine).

### Integration Points
- `ReviewDocument.tsx` isMermaidSection branch → replace `<pre>` + note with
  `<MermaidDiagram source={...} />` component.
- `ReviewPanel.ts` CSP string → add `'unsafe-eval'` to script-src.
- `vite.config.ts` → mermaid may need to be excluded from tree-shaking or handled
  as a dynamic import to control bundle splitting.

</code_context>

<deferred>
## Deferred Ideas

None — discussion stayed within phase scope.

</deferred>

---

*Phase: 02.3-review-panel-rich-rendering*
*Context gathered: 2026-04-04*
