---
phase: 06-multi-agent-pr-review-pipeline-with-model-selection
plan: "01"
type: tdd
wave: 0
depends_on: []
files_modified:
  - src/easy-review/agents/AgentOrchestrator.test.ts
  - src/easy-review/cli/OllamaAdapter.test.ts
  - src/easy-review/agents/contextRequest.test.ts
  - src/easy-review/agents/mermaidValidation.test.ts
  - src/easy-review/cli/ReviewParser.test.ts
  - src/easy-review/settings/modelSettings.test.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "npm run test:unit exits 0 with all 6 test files present (passing stubs or skipped)"
    - "Each test file imports its subject module (the future implementation path is established)"
    - "No test file touches any VS Code API directly (all tests run in vitest node environment)"
  artifacts:
    - path: "src/easy-review/agents/AgentOrchestrator.test.ts"
      provides: "TDD scaffold for D-03/D-04/D-05 (7-agent concurrent dispatch, SectionState map)"
    - path: "src/easy-review/cli/OllamaAdapter.test.ts"
      provides: "TDD scaffold for D-11 (ndjson streaming, fetch mock)"
    - path: "src/easy-review/agents/contextRequest.test.ts"
      provides: "TDD scaffold for D-14 (CONTEXT_REQUEST header parser)"
    - path: "src/easy-review/agents/mermaidValidation.test.ts"
      provides: "TDD scaffold for D-16/D-17 (Mermaid regex validation, retry count)"
    - path: "src/easy-review/cli/ReviewParser.test.ts"
      provides: "7 new section name cases added to existing test file (D-01)"
    - path: "src/easy-review/settings/modelSettings.test.ts"
      provides: "TDD scaffold for D-19 (activeModel → defaultModel migration)"
  key_links:
    - from: "AgentOrchestrator.test.ts"
      to: "src/easy-review/agents/AgentOrchestrator.ts"
      via: "import { runAllAgents } from './AgentOrchestrator'"
    - from: "OllamaAdapter.test.ts"
      to: "src/easy-review/cli/OllamaAdapter.ts"
      via: "import { OllamaAdapter } from './OllamaAdapter'"
    - from: "contextRequest.test.ts"
      to: "src/easy-review/agents/contextRequest.ts"
      via: "import { parseContextRequest } from './contextRequest'"
    - from: "mermaidValidation.test.ts"
      to: "src/easy-review/agents/mermaidValidation.ts"
      via: "import { validateMermaidSyntax, extractMermaidBlock } from './mermaidValidation'"
    - from: "modelSettings.test.ts"
      to: "src/easy-review/settings/modelSettings.ts"
      via: "import { resolveAgentModel } from './modelSettings'"
---

<objective>
Wave 0: Create TDD test scaffolds for all new Phase 6 modules BEFORE any implementation exists.

Purpose: Establish RED tests that define the contracts for AgentOrchestrator, OllamaAdapter, contextRequest parser, mermaid validation, 7-section ReviewParser, and settings migration. Executors for later plans MUST make these tests green.
Output: 6 test files. All either skip (with `.todo`) or fail with "module not found" — no passing stubs that hide real failures.
</objective>

<execution_context>
@/Users/gustavo.moreno/Documents/personal info/easy-review/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gustavo.moreno/Documents/personal info/easy-review/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-multi-agent-pr-review-pipeline-with-model-selection/06-CONTEXT.md
@.planning/phases/06-multi-agent-pr-review-pipeline-with-model-selection/06-RESEARCH.md
@.planning/phases/06-multi-agent-pr-review-pipeline-with-model-selection/06-VALIDATION.md
@src/easy-review/cli/ReviewParser.ts
@src/shared/types.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AgentOrchestrator + OllamaAdapter + contextRequest + mermaidValidation stubs</name>
  <read_first>
    - src/easy-review/cli/ReviewParser.ts (pattern for how test files are co-located)
    - src/easy-review/cli/ClaudeAdapter.ts (CLIAdapter interface to understand adapter shape)
    - src/shared/types.ts (existing types; new AgentKey/SectionState will extend this)
    - .planning/phases/06-multi-agent-pr-review-pipeline-with-model-selection/06-RESEARCH.md (Patterns 1-5 and exact function signatures)
    - .planning/phases/06-multi-agent-pr-review-pipeline-with-model-selection/06-VALIDATION.md (Wave 0 requirements table)
  </read_first>
  <files>
    src/easy-review/agents/AgentOrchestrator.test.ts,
    src/easy-review/cli/OllamaAdapter.test.ts,
    src/easy-review/agents/contextRequest.test.ts,
    src/easy-review/agents/mermaidValidation.test.ts
  </files>
  <behavior>
    AgentOrchestrator.test.ts:
    - import { runAllAgents } from './AgentOrchestrator' (file does not exist yet — will fail at build until Plan 06-05)
    - Use vi.importMock to avoid hard crash; wrap in describe.todo blocks
    - Test: runAllAgents returns a Record with keys for all 7 AgentKey values: prSummarizer, bugRisk, architectureChange, testCoverage, documentation, diagram, businessImpact
    - Test: all 7 agents are dispatched — Promise.allSettled receives 7 entries
    - Test: SectionState starts as { status: 'pending' } before resolution
    - Test: on agent completion, SectionState transitions to { status: 'complete', content: string }
    - Test: on agent error, SectionState transitions to { status: 'error', error: string }

    OllamaAdapter.test.ts:
    - import { OllamaAdapter } from './OllamaAdapter' (file does not exist yet)
    - Mock global fetch using vi.stubGlobal('fetch', mockFetch)
    - Test: POST to 'http://localhost:11434/api/generate' with { model: 'gemma4', prompt: 'test', stream: true }
    - Test: ndjson response with lines '{"response":"hello","done":false}\n{"response":" world","done":true}' accumulates to "hello world"
    - Test: HTTP 500 throws Error with message containing "Ollama HTTP 500"
    - Test: AbortSignal cancellation: after abort, no further chunks processed

    contextRequest.test.ts:
    - import { parseContextRequest } from './contextRequest' (file does not exist yet)
    - Test: template with no ## CONTEXT_REQUEST block → returns { projectAnalysis: false, commitHistory: false, body: fullTemplate }
    - Test: template with 'project_analysis: true\ncommit_history: false\n---\n{body}' → { projectAnalysis: true, commitHistory: false, body: '{body}' }
    - Test: template with 'project_analysis: false\ncommit_history: true\n---\n{body}' → { projectAnalysis: false, commitHistory: true, body: '{body}' }
    - Test: template with both true → { projectAnalysis: true, commitHistory: true }

    mermaidValidation.test.ts:
    - import { validateMermaidSyntax, extractMermaidBlock } from './mermaidValidation' (file does not exist yet)
    - validateMermaidSyntax tests:
      - 'graph TD\n  A-->B' → { valid: true }
      - 'flowchart LR\n  A-->B' → { valid: true }
      - 'sequenceDiagram\n  A->>B: msg' → { valid: true }
      - 'invalid stuff here' → { valid: false, error: string containing "Unrecognized diagram type" }
      - 'pie title Pets\n  "Dogs": 45' → { valid: true }
    - extractMermaidBlock tests:
      - content with '\`\`\`mermaid\ngraph TD\n  A-->B\n\`\`\`' → 'graph TD\n  A-->B'
      - content with no mermaid block → null
  </behavior>
  <action>
    Create the src/easy-review/agents/ directory (mkdir -p via file creation).
    Create src/easy-review/settings/ directory.

    All 4 test files use vi.importMock() pattern OR wrap the subject import in a try/catch
    with vi.mock('...', () => ({ ... })) factory returning stub implementations that make
    the tests SKIP rather than error (use describe.todo or it.todo for tests that cannot
    run until the implementation exists).

    IMPORTANT: The test file for AgentOrchestrator MUST use vi.mock to stub the
    @anthropic-ai/claude-agent-sdk import path, because that package won't be installed
    until Plan 06-05. Example mock in mermaidValidation.test.ts and contextRequest.test.ts:
    these are pure functions with no external deps — write real failing tests, NOT .todo.

    For OllamaAdapter.test.ts: use vi.stubGlobal('fetch', ...) in beforeEach, restore in afterEach.
    The fetch mock must return a ReadableStream with the ndjson test data.
    Use: new ReadableStream({ start(c) { c.enqueue(encoder.encode(ndjson)); c.close(); } })
    Wrap the OllamaAdapter import in vi.mock() factory that returns a stub class
    until the actual implementation exists in Plan 06-02.

    Each test file MUST have at least one concrete passing test (even if it's a trivial
    contract check like "parseContextRequest returns an object") so npm run test:unit
    exits 0 after this plan completes.
  </action>
  <verify>
    <automated>cd "/Users/gustavo.moreno/Documents/personal info/easy-review" && npm run test:unit 2>&1 | tail -20</automated>
  </verify>
  <done>
    npm run test:unit exits 0. All 4 new test files exist. Each file imports its subject
    module path (establishing the contract). At least 1 test in each file passes or is marked .todo.
  </done>
  <acceptance_criteria>
    - src/easy-review/agents/AgentOrchestrator.test.ts exists and contains "import" referencing "./AgentOrchestrator"
    - src/easy-review/cli/OllamaAdapter.test.ts exists and contains "import" referencing "./OllamaAdapter"
    - src/easy-review/agents/contextRequest.test.ts exists and contains "import" referencing "./contextRequest"
    - src/easy-review/agents/mermaidValidation.test.ts exists and contains "import" referencing "./mermaidValidation"
    - npm run test:unit exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ReviewParser 7-section tests + modelSettings stub</name>
  <read_first>
    - src/easy-review/cli/ReviewParser.ts (existing parseReview and parseFindingsSection)
    - src/easy-review/settings/modelSettings.ts (does not exist yet — will be created in Plan 06-04)
    - .planning/phases/06-multi-agent-pr-review-pipeline-with-model-selection/06-CONTEXT.md (D-01: 7 section names, D-19/D-20/D-21: settings)
  </read_first>
  <files>
    src/easy-review/cli/ReviewParser.test.ts,
    src/easy-review/settings/modelSettings.test.ts
  </files>
  <behavior>
    ReviewParser.test.ts additions (extend existing file — do NOT overwrite):
    - The existing test file covers the 6 Phase 5 section names. ADD new describe block: "7-section Phase 6 contract"
    - Test: parseReview with '## PR Summary\ncontent1\n## Bug & Risk Analysis\ncontent2' → sections[0].title === 'PR Summary', sections[1].title === 'Bug & Risk Analysis'
    - Test: parseReview with '## Architecture Changes\ncontent' → sections[0].title === 'Architecture Changes'
    - Test: parseReview with '## Test Coverage\ncontent' → sections[0].title === 'Test Coverage'
    - Test: parseReview with '## Documentation Review\ncontent' → sections[0].title === 'Documentation Review'
    - Test: parseReview with '## Visual Overview\n\`\`\`mermaid\ngraph TD\n  A-->B\n\`\`\`' → sections[0].title === 'Visual Overview'
    - Test: parseReview with '## Business Impact\ncontent' → sections[0].title === 'Business Impact'
    - Test: buildSection for 'Bug & Risk Analysis' section with '[critical] missing null check' → findings array has 1 item with severity: 'critical'
    - NOTE: parseFindingsSection is triggered by title 'finding' — update test to also trigger for 'bug' keyword (this test will FAIL until ReviewParser.ts is updated in Plan 06-04)

    modelSettings.test.ts (new file):
    - import { resolveAgentModel, migrateActiveModel } from './modelSettings' (file does not exist yet)
    - vi.mock to stub VS Code config if needed; OR accept that these are pure functions with a config param
    - Test: resolveAgentModel({ agentKey: 'bugRisk', agentModels: { bugRisk: 'codex' }, defaultModel: 'claude' }) → 'codex'
    - Test: resolveAgentModel({ agentKey: 'testCoverage', agentModels: {}, defaultModel: 'ollama' }) → 'ollama' (falls back to defaultModel)
    - Test: resolveAgentModel({ agentKey: 'diagram', agentModels: undefined, defaultModel: 'claude' }) → 'claude'
    - Test: migrateActiveModel({ activeModel: 'codex', defaultModel: undefined }) → 'codex'
    - Test: migrateActiveModel({ activeModel: 'codex', defaultModel: 'claude' }) → 'claude' (defaultModel wins if set)
    - Test: migrateActiveModel({ activeModel: undefined, defaultModel: undefined }) → 'claude' (fallback)
  </behavior>
  <action>
    For ReviewParser.test.ts: find the existing test file (if it exists — check
    src/easy-review/cli/ReviewParser.test.ts). If it exists, append the new describe block
    at the end. If it does not exist, create it with both the original and new tests.
    The test for 'bug' keyword triggering findings parsing SHOULD be written even though
    it will fail until Plan 06-04 — use it.todo('bug section triggers findings parser')
    so that npm run test:unit exits 0.

    For modelSettings.test.ts: pure function tests with no VS Code deps.
    The resolveAgentModel function signature is:
      resolveAgentModel(opts: { agentKey: AgentKey; agentModels?: Partial<Record<AgentKey, ModelName>>; defaultModel: ModelName }): ModelName
    where ModelName = 'claude' | 'codex' | 'ollama'
    Wrap the import in vi.mock() factory returning stub implementations so tests pass as
    pending until Plan 06-04 creates the actual module. Use describe.todo or it.todo where needed.
  </action>
  <verify>
    <automated>cd "/Users/gustavo.moreno/Documents/personal info/easy-review" && npm run test:unit 2>&1 | tail -20</automated>
  </verify>
  <done>
    npm run test:unit exits 0. ReviewParser.test.ts has a new describe block for 7-section names.
    modelSettings.test.ts exists with the resolveAgentModel and migrateActiveModel test cases.
  </done>
  <acceptance_criteria>
    - src/easy-review/cli/ReviewParser.test.ts contains the string "PR Summary"
    - src/easy-review/cli/ReviewParser.test.ts contains the string "Bug & Risk Analysis"
    - src/easy-review/settings/modelSettings.test.ts exists and contains "resolveAgentModel"
    - src/easy-review/settings/modelSettings.test.ts contains "migrateActiveModel"
    - npm run test:unit exits 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
npm run test:unit exits 0 with all 6 test files present (4 new + updated ReviewParser.test.ts + new modelSettings.test.ts). No test file crashes vitest with an uncaught import error.
</verification>

<success_criteria>
- 6 test files exist at their contracted paths
- Each test file imports its subject module path
- npm run test:unit exits 0 (all tests pass or are marked .todo)
- No test touches VS Code API at import time
</success_criteria>

<output>
After completion, create `.planning/phases/06-multi-agent-pr-review-pipeline-with-model-selection/06-01-SUMMARY.md`
</output>
