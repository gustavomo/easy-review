---
phase: 06-multi-agent-pr-review-pipeline-with-model-selection
plan: "03"
type: execute
wave: 1
depends_on: ["06-01"]
files_modified:
  - src/easy-review/agents/contextRequest.ts
  - src/easy-review/agents/mermaidValidation.ts
  - src/easy-review/agents/PRSummarizerAgent.ts
  - src/easy-review/agents/BugRiskAgent.ts
  - src/easy-review/agents/ArchitectureChangeAgent.ts
  - src/easy-review/agents/TestCoverageAgent.ts
  - src/easy-review/agents/DocumentationAgent.ts
  - src/easy-review/agents/DiagramAgent.ts
  - src/easy-review/agents/BusinessImpactAgent.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "parseContextRequest correctly parses ## CONTEXT_REQUEST header from template strings"
    - "validateMermaidSyntax returns valid: true for all 15 Mermaid diagram type keywords"
    - "extractMermaidBlock extracts mermaid source from triple-backtick fences"
    - "All 7 agent files export a getSystemPrompt() function and a getTemplate(opts) function"
    - "Each agent template includes the agent's specific review focus"
    - "Only BugRiskAgent and ArchitectureChangeAgent request project_analysis: true"
    - "contextRequest.test.ts and mermaidValidation.test.ts both pass (not .todo)"
  artifacts:
    - path: "src/easy-review/agents/contextRequest.ts"
      provides: "parseContextRequest() pure function"
      exports: ["parseContextRequest", "ContextRequest"]
    - path: "src/easy-review/agents/mermaidValidation.ts"
      provides: "validateMermaidSyntax() + extractMermaidBlock() pure functions"
      exports: ["validateMermaidSyntax", "extractMermaidBlock", "MERMAID_DIAGRAM_TYPES"]
    - path: "src/easy-review/agents/DiagramAgent.ts"
      provides: "Diagram agent template with mermaid output instruction"
      exports: ["DiagramAgent"]
  key_links:
    - from: "src/easy-review/agents/AgentOrchestrator.ts"
      to: "src/easy-review/agents/contextRequest.ts"
      via: "parseContextRequest(template) before dispatching each agent"
      pattern: "parseContextRequest"
    - from: "src/easy-review/agents/AgentOrchestrator.ts"
      to: "src/easy-review/agents/mermaidValidation.ts"
      via: "validateMermaidSyntax after DiagramAgent completes"
      pattern: "validateMermaidSyntax"
---

<objective>
Wave 1 (parallel with 06-02): Implement pure utility modules and per-agent prompt templates.

Purpose: contextRequest.ts and mermaidValidation.ts are the orchestrator's parsing utilities — no external deps, pure functions, can be tested fully in vitest. The 7 agent files define the prompt templates for the parallel review pipeline.
Output: 9 files — 2 utility modules + 7 agent template files.
</objective>

<execution_context>
@/Users/gustavo.moreno/Documents/personal info/easy-review/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gustavo.moreno/Documents/personal info/easy-review/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-multi-agent-pr-review-pipeline-with-model-selection/06-CONTEXT.md
@.planning/phases/06-multi-agent-pr-review-pipeline-with-model-selection/06-RESEARCH.md
@src/easy-review/cli/PromptBuilder.ts
@.planning/phases/05-upgrade-review-prompt-to-generate-deep-insight-rich-pr-analysis/05-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: contextRequest.ts + mermaidValidation.ts utility modules</name>
  <read_first>
    - src/easy-review/agents/contextRequest.test.ts (from Plan 06-01 — the contracts are defined there)
    - src/easy-review/agents/mermaidValidation.test.ts (from Plan 06-01 — the contracts are defined there)
    - .planning/phases/06-multi-agent-pr-review-pipeline-with-model-selection/06-RESEARCH.md (Pattern 3: contextRequest regex; Pattern 4: MERMAID_DIAGRAM_TYPES list + validateMermaidSyntax)
    - .planning/phases/06-multi-agent-pr-review-pipeline-with-model-selection/06-CONTEXT.md (D-14: CONTEXT_REQUEST header format; D-16: extension-host mermaid validation)
  </read_first>
  <files>
    src/easy-review/agents/contextRequest.ts,
    src/easy-review/agents/mermaidValidation.ts
  </files>
  <behavior>
    contextRequest.ts:
    - parseContextRequest(template: string): ContextRequest
    - ContextRequest = { projectAnalysis: boolean; commitHistory: boolean; body: string }
    - Header format: '## CONTEXT_REQUEST\nproject_analysis: true\ncommit_history: false\n---\n{body}'
    - Regex from RESEARCH.md Pattern 3: /^##\s+CONTEXT_REQUEST\s*\n([\s\S]*?)\n---\s*\n([\s\S]*)$/
    - If no header block: return { projectAnalysis: false, commitHistory: false, body: template }
    - projectAnalysis: /project_analysis:\s*true/i test on headerBlock
    - commitHistory: /commit_history:\s*true/i test on headerBlock
    - body: everything after the '---\n' line

    mermaidValidation.ts:
    - MERMAID_DIAGRAM_TYPES: string[] — exact list from RESEARCH.md Pattern 4:
      ['graph', 'flowchart', 'sequenceDiagram', 'classDiagram', 'stateDiagram',
       'stateDiagram-v2', 'erDiagram', 'journey', 'gantt', 'pie', 'gitGraph',
       'mindmap', 'timeline', 'xychart-beta', 'block-beta', 'quadrantChart']
    - validateMermaidSyntax(source: string): { valid: boolean; error?: string }
      - firstLine = source.trim().split('\n')[0].trim()
      - Check if firstLine.toLowerCase().startsWith(type.toLowerCase()) for any type in list
      - If not found: return { valid: false, error: `Unrecognized diagram type: "${firstLine}". Expected one of: ${MERMAID_DIAGRAM_TYPES.join(', ')}` }
      - If found: return { valid: true }
    - extractMermaidBlock(content: string): string | null
      - Match: /```mermaid\s*\n([\s\S]*?)```/ — return group 1 trimmed
      - If no match: return null
  </behavior>
  <action>
    Implement the two modules as pure TypeScript functions with no external dependencies
    and no vscode or Node.js imports (they must be testable in vitest node environment).

    After implementation, run the test files from Plan 06-01 to verify they now pass
    (these test files were written against these exact function signatures).

    contextRequest.ts export signature:
    ```typescript
    export interface ContextRequest {
      projectAnalysis: boolean;
      commitHistory: boolean;
      body: string;
    }
    export function parseContextRequest(template: string): ContextRequest
    ```

    mermaidValidation.ts export signature:
    ```typescript
    export const MERMAID_DIAGRAM_TYPES: string[]
    export function validateMermaidSyntax(source: string): { valid: boolean; error?: string }
    export function extractMermaidBlock(content: string): string | null
    ```
  </action>
  <verify>
    <automated>cd "/Users/gustavo.moreno/Documents/personal info/easy-review" && npm run test:unit -- --reporter=verbose 2>&1 | grep -E "contextRequest|mermaidValidation|PASS|FAIL" | head -30</automated>
  </verify>
  <done>
    contextRequest.test.ts and mermaidValidation.test.ts both pass (no .todo skips). npm run test:unit exits 0.
  </done>
  <acceptance_criteria>
    - src/easy-review/agents/contextRequest.ts contains "export function parseContextRequest"
    - src/easy-review/agents/contextRequest.ts contains "export interface ContextRequest"
    - src/easy-review/agents/mermaidValidation.ts contains "export const MERMAID_DIAGRAM_TYPES"
    - src/easy-review/agents/mermaidValidation.ts contains "'sequenceDiagram'" AND "'classDiagram'" AND "'stateDiagram-v2'"
    - src/easy-review/agents/mermaidValidation.ts contains "export function validateMermaidSyntax"
    - src/easy-review/agents/mermaidValidation.ts contains "export function extractMermaidBlock"
    - npm run test:unit exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: 7 per-agent prompt template files</name>
  <read_first>
    - src/easy-review/cli/PromptBuilder.ts (SYNTHESIS_INSTRUCTION — per-agent prompts inherit this quality bar; read QUALITY_LEVEL and ANNOTATION RULES sections)
    - .planning/phases/05-upgrade-review-prompt-to-generate-deep-insight-rich-pr-analysis/05-CONTEXT.md (D-01 through D-12 — the quality bar; D-03 annotation rules; D-07 findings format; D-09 mermaid output format)
    - .planning/phases/06-multi-agent-pr-review-pipeline-with-model-selection/06-CONTEXT.md (D-01: section names; D-13: agent directory structure; D-14: CONTEXT_REQUEST format; D-15: which agents request context)
    - .planning/phases/06-multi-agent-pr-review-pipeline-with-model-selection/06-RESEARCH.md (Architecture Patterns section: recommended project structure and ADK query() pattern)
  </read_first>
  <files>
    src/easy-review/agents/PRSummarizerAgent.ts,
    src/easy-review/agents/BugRiskAgent.ts,
    src/easy-review/agents/ArchitectureChangeAgent.ts,
    src/easy-review/agents/TestCoverageAgent.ts,
    src/easy-review/agents/DocumentationAgent.ts,
    src/easy-review/agents/DiagramAgent.ts,
    src/easy-review/agents/BusinessImpactAgent.ts
  </files>
  <action>
    Each agent file exports:
    1. getSystemPrompt(): string — the short system-level instruction for this agent
    2. getTemplate(opts: AgentTemplateOpts): string — the full prompt including ## CONTEXT_REQUEST header

    AgentTemplateOpts interface (define inline or in a shared agents/types.ts):
    ```typescript
    export interface AgentTemplateOpts {
      diff: string;          // full PR diff text
      fileList: string;      // newline-separated list of changed files
      projectAnalysis?: string;  // injected by orchestrator if declared in CONTEXT_REQUEST
      commitHistory?: string;    // injected by orchestrator if declared in CONTEXT_REQUEST
    }
    ```

    CONTEXT_REQUEST headers (per D-14/D-15):
    - PRSummarizerAgent: no CONTEXT_REQUEST (omit block)
    - BugRiskAgent: project_analysis: true, commit_history: false
    - ArchitectureChangeAgent: project_analysis: true, commit_history: false
    - TestCoverageAgent: no CONTEXT_REQUEST
    - DocumentationAgent: no CONTEXT_REQUEST
    - DiagramAgent: no CONTEXT_REQUEST
    - BusinessImpactAgent: project_analysis: true, commit_history: false

    Section headings (per D-01) — each agent's output MUST start with the section heading:
    - PRSummarizerAgent → ## PR Summary
    - BugRiskAgent → ## Bug & Risk Analysis
    - ArchitectureChangeAgent → ## Architecture Changes
    - TestCoverageAgent → ## Test Coverage
    - DocumentationAgent → ## Documentation Review
    - DiagramAgent → ## Visual Overview
    - BusinessImpactAgent → ## Business Impact

    DiagramAgent specific requirements (D-16):
    - Must instruct the model to output a Mermaid diagram wrapped in ```mermaid fences
    - Must instruct: "Produce a syntactically valid Mermaid diagram. Start the diagram with a recognized type keyword (graph, flowchart, sequenceDiagram, classDiagram, etc.)"
    - Must produce output under the ## Visual Overview section heading

    BugRiskAgent specific requirements:
    - Output format must use finding markers: [critical], [warning], [suggestion] at the start of each finding
    - Each finding on its own line starting with the severity marker

    Quality bar (from Phase 5):
    - Avoid vague hedging language ("could potentially", "might possibly")
    - Quote specific function names, file paths, line patterns from the diff
    - Cite concrete evidence from the diff, not general statements

    Each agent file structure:
    ```typescript
    import type { AgentTemplateOpts } from './agentTypes';

    export function getSystemPrompt(): string {
      return `You are a specialized code reviewer focusing on [FOCUS_AREA].
    [2-3 sentences defining the agent's scope and output format]`;
    }

    export function getTemplate(opts: AgentTemplateOpts): string {
      const contextBlock = opts.projectAnalysis
        ? `\n\n## Project Context\n${opts.projectAnalysis}`
        : '';
      return `[## CONTEXT_REQUEST block if needed]

    ## Changed Files
    ${opts.fileList}

    ## Diff
    \`\`\`diff
    ${opts.diff}
    \`\`\`${contextBlock}

    ---
    [Agent-specific instructions]
    Begin your response with "## [Section Name]"
    `;
    }
    ```

    Also create src/easy-review/agents/agentTypes.ts with the AgentTemplateOpts interface.
  </action>
  <verify>
    <automated>cd "/Users/gustavo.moreno/Documents/personal info/easy-review" && npx tsc --noEmit 2>&1 | grep "agents/" | head -20</automated>
  </verify>
  <done>
    All 7 agent files exist and compile without errors. Each exports getSystemPrompt() and getTemplate(opts). DiagramAgent template includes "mermaid" and diagram type instruction. BugRiskAgent template includes "[critical]", "[warning]", "[suggestion]" format guidance.
  </done>
  <acceptance_criteria>
    - src/easy-review/agents/PRSummarizerAgent.ts contains "export function getSystemPrompt" AND "export function getTemplate"
    - src/easy-review/agents/BugRiskAgent.ts contains "## CONTEXT_REQUEST" AND "project_analysis: true"
    - src/easy-review/agents/BugRiskAgent.ts contains "[critical]" AND "[warning]" AND "[suggestion]"
    - src/easy-review/agents/ArchitectureChangeAgent.ts contains "## CONTEXT_REQUEST" AND "project_analysis: true"
    - src/easy-review/agents/DiagramAgent.ts contains "mermaid" AND "Visual Overview"
    - src/easy-review/agents/BusinessImpactAgent.ts contains "## CONTEXT_REQUEST" AND "project_analysis: true"
    - src/easy-review/agents/agentTypes.ts contains "export interface AgentTemplateOpts"
    - npx tsc --noEmit exits 0 (for agents/ directory at minimum)
  </acceptance_criteria>
</task>

</tasks>

<verification>
npm run test:unit exits 0. contextRequest.test.ts and mermaidValidation.test.ts pass fully (no .todo). All 7 agent files compile without TypeScript errors.
</verification>

<success_criteria>
- parseContextRequest() passes all test cases from contextRequest.test.ts
- validateMermaidSyntax() and extractMermaidBlock() pass all test cases from mermaidValidation.test.ts
- 7 agent files + agentTypes.ts exist with correct exports
- CONTEXT_REQUEST headers match D-14/D-15 spec exactly
- Section headings match D-01 spec exactly
</success_criteria>

<output>
After completion, create `.planning/phases/06-multi-agent-pr-review-pipeline-with-model-selection/06-03-SUMMARY.md`
</output>
