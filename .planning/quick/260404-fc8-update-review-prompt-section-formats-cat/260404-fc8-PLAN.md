---
phase: quick
plan: 260404-fc8
type: execute
wave: 1
depends_on: []
files_modified:
  - src/easy-review/cli/PromptBuilder.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "Categorized Changes section instructs Claude to output a markdown table (Category | Files Changed | Description)"
    - "Code Review Findings section instructs Claude to output a flat prefixed list with bold severity tags"
    - "Impact Analysis section instructs Claude to output a markdown table (Area | Risk | Notes)"
    - "Executive Summary, Key Code Changes, Visual Overview, and RULES sections are unchanged"
  artifacts:
    - path: "src/easy-review/cli/PromptBuilder.ts"
      provides: "Updated SYNTHESIS_INSTRUCTION with new section format instructions"
  key_links:
    - from: "SYNTHESIS_INSTRUCTION const"
      to: "buildPrompt()"
      via: "parts.push(SYNTHESIS_INSTRUCTION)"
      pattern: "SYNTHESIS_INSTRUCTION"
---

<objective>
Update three section format instructions in `SYNTHESIS_INSTRUCTION` inside `PromptBuilder.ts`.

Purpose: The current instructions produce bulleted/paragraph output for Categorized Changes, Code Review Findings, and Impact Analysis. Switching to tables and a flat prefixed list makes these sections machine-parseable by the existing React components that already handle table and structured content.
Output: Modified `PromptBuilder.ts` with updated prompt text for the three target sections only.
</objective>

<execution_context>
@/Users/gustavo.moreno/Documents/personal info/easy-review/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gustavo.moreno/Documents/personal info/easy-review/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@src/easy-review/cli/PromptBuilder.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update three section format instructions in SYNTHESIS_INSTRUCTION</name>
  <files>src/easy-review/cli/PromptBuilder.ts</files>
  <action>
Replace exactly three sections inside the `SYNTHESIS_INSTRUCTION` template literal. Leave all other sections (Executive Summary, Key Code Changes, Visual Overview, RULES) byte-for-byte identical.

**Section 1 — Replace `## Categorized Changes`**

Current text (lines 58–68 of the file):
```
## Categorized Changes
Group changed files by type with a short description of WHAT each file's
change accomplishes (not just "modified"):
- **Features**: New functionality
- **Fixes**: Bug fixes
- **Refactors / Cleanup**: Removals, restructuring, dead code elimination
- **Tests**: Test additions or modifications
- **Config / Infra**: Build, CI, environment changes

For each file, one line: \`path/to/file\` — what this specific change does.
Include full GitHub PR file links where available.
```

Replace with:
```
## Categorized Changes
Output a markdown table listing every changed file. Use these category values
only: Feature, Bug Fix, Refactor/Cleanup, Tests, Config/Infra.
One row per file (or group of files with identical change). Include full
GitHub PR file links in the Files Changed cell where available.

| Category | Files Changed | Description |
|---|---|---|
| Bug Fix | \`path/to/file.ts\` | What this change does |
| Tests | \`path/to/file.spec.ts\` | What this test covers |
```

**Section 2 — Replace `## Code Review Findings`**

Current text (lines 117–120 of the file):
```
## Code Review Findings
Findings from review comments (if any). Group by severity: critical, warning,
suggestion. If no review comments exist, write: "No review comments on this PR."
Do NOT invent findings — only include what is in the data.
```

Replace with:
```
## Code Review Findings
Output a flat list where each item is prefixed with a bold severity tag.
Severity values: critical, warning, suggestion.
If no review comments exist, write exactly: "No review comments on this PR."
Do NOT invent findings — only include what is in the data.

**[critical]** \`file.ts:line\` — description
**[warning]** \`file.ts:line\` — description
**[suggestion]** \`file.ts\` — description
```

**Section 3 — Replace `## Impact Analysis`**

Current text (lines 122–125 of the file):
```
## Impact Analysis
- **Risk**: low / medium / high — justify in one sentence
- **Areas affected**: which features, layers, or user flows are impacted
- **Side effects**: anything downstream that depends on the changed code
```

Replace with:
```
## Impact Analysis
Output a markdown table. Each impacted area gets its own row.
Risk column uses only: Low, Low-positive, Medium, High, Positive.

| Area | Risk | Notes |
|---|---|---|
| Area name | Low/Medium/High/Positive | One sentence note |
```

After editing, verify the surrounding sections (Executive Summary above Categorized Changes, Key Code Changes below it, Visual Overview, RULES) are character-for-character unchanged from the original file.
  </action>
  <verify>
    <automated>cd "/Users/gustavo.moreno/Documents/personal info/easy-review" && npx tsc --noEmit --project tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>
    - `SYNTHESIS_INSTRUCTION` contains "| Category | Files Changed | Description |" table header for Categorized Changes
    - `SYNTHESIS_INSTRUCTION` contains "**[critical]**" prefixed list format for Code Review Findings
    - `SYNTHESIS_INSTRUCTION` contains "| Area | Risk | Notes |" table header for Impact Analysis
    - TypeScript compile passes with no new errors
    - Executive Summary, Key Code Changes, Visual Overview, and RULES sections are identical to the original file
  </done>
</task>

</tasks>

<verification>
- `grep -n "Category | Files Changed" src/easy-review/cli/PromptBuilder.ts` returns a match
- `grep -n "\*\*\[critical\]\*\*" src/easy-review/cli/PromptBuilder.ts` returns a match
- `grep -n "Area | Risk | Notes" src/easy-review/cli/PromptBuilder.ts` returns a match
- `grep -n "Group changed files by type" src/easy-review/cli/PromptBuilder.ts` returns no match (old Categorized Changes removed)
- `grep -n "Group by severity" src/easy-review/cli/PromptBuilder.ts` returns no match (old Findings removed)
- `grep -n "Areas affected" src/easy-review/cli/PromptBuilder.ts` returns no match (old Impact Analysis removed)
- TypeScript compile is clean
</verification>

<success_criteria>
Three sections in `SYNTHESIS_INSTRUCTION` produce table/prefixed-list output instructions. All other sections unchanged. File compiles without errors.
</success_criteria>

<output>
After completion, create `.planning/quick/260404-fc8-update-review-prompt-section-formats-cat/260404-fc8-SUMMARY.md` using the summary template.
</output>
