import { describe, it } from 'vitest';

// Wave 0 stub — EasyReviewDiffContentProvider does not exist yet (Plan 02.1-03)
// TODO: import { encodeDiffUri, decodeDiffUri } from './EasyReviewDiffContentProvider';

describe('EasyReviewDiffContentProvider', () => {
  it.todo('encodeDiffUri / decodeDiffUri round-trip');
  it.todo('provider returns correct content for diff URI');
  it.todo('provider returns empty buffer for unknown URI');
});
